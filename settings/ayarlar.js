require("dotenv").config();

module.exports = {
    TOKEN: process.env.TOKEN || "BOT-TOKEN-ADD",  // bot token gir :)
    PREFIX: process.env.PREFIX || "PREFİX-ADD", // bot prefix
    SAHİP: process.env.SAHİP || "SAHİP-ID-ADD", //sahip id :()
}